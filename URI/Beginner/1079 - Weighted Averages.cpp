#include <cstdio>
using namespace std;

int t;
float n1, n2, n3, avg;

int main () {

	scanf("%d", &t);
	
	while (t--) {
		scanf("%f %f %f", &n1, &n2, &n3);
		media = ((n1 * 2.0) + (n2 * 3.0) + (n3 * 5.0)) / 10.0;
		printf("%.1f\n", avg);
	}

	return 0;
	
}
